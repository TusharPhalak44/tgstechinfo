// Placeholder for contentRoute navigation
export const navigateContentItem = (item) => {
  console.log('navigateContentItem called with:', item);
  // This function was imported but never used in Home.jsx
  // Keeping this as a placeholder to prevent import errors
};
